<%- include("partials/header", { title: "Instructors", pageCss: "/assets/css/admin/users.css" }) %>
<%- include("partials/sidebar", { activeTab }) %>

<div class="pageHead">
  <div class="searchBox">
    <input id="searchInstructors" type="text" placeholder="Search instructors..." />
  </div>
</div>

<div class="tableWrap">
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Phone</th>
      </tr>
    </thead>

    <tbody id="instructorsBody">
      <!-- Data loaded via JS -->
    </tbody>
  </table>
</div>

<%- include("partials/footer", { pageJs: "/assets/js/admin/instructors.js" }) %>
